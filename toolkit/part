#!/usr/bin/env perl

use 5.010_000;
use strict;
use warnings;

use Getopt::Long qw/:config no_ignore_case /;
use Pod::Usage;
use File::Find;

my %obj;

GetOptions( 'help|?|h' => \$obj{options}->{'help'} );

pod2usage "\nMUTATE :: Partioner\n"
  if defined $obj{options}->{help}
      or not $ARGV[0];

__END__

=head1 SYNOPSIS

"command line" [options]

Options:

  -h, -?, --help  This help / usage screen...

Example: mutate part "command line"

=cut
